services:
  - type: web
    name: product-management-api
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: MCP_API_KEY
        sync: false